<h3>Comments :</h3>
<% obj.comments.where(approved: true).each do |comment| %>
  <div class="card">
    <div class="card-body">
      <%= render "comments/comment",  comment: comment %>
    </div>
  </div>
<% end %>
